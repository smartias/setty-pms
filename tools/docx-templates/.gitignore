# Generated tokenized templates and rendered demos.
# The tokenized templates belong in SharePoint next to the originals, where the
# edge function reads them via Graph at runtime -- not in git as ~500KB binaries.
build/
__pycache__/
